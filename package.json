{
  "name": "json-to-custom-env",
  "version": "1.1.0",
  "description": "Creates a Custom Env file for Config.js from a JSON file",
  "main": "index.js",
  "scripts": {
    "security": "nsp check",
    "lint": "standard",
    "test": "NODE_ENV=test mocha",
    "cover": "NODE_ENV=test istanbul cover _mocha -- -R tap > test-reports.tap && istanbul report html"
  },
  "bin": {
    "json2env": "./bin/index.js"
  },
  "standard": {
    "global": [
      "describe",
      "it",
      "before"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jsonfile": "^3.0.0"
  }
}
